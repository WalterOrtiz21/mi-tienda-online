# docker-compose.yml
version: '3.8'

services:
  # Servicio de la Base de Datos MySQL
  db:
    image: mysql:8.0
    container_name: annyamodas-db
    restart: always
    command: >
      --character-set-server=utf8mb4
      --collation-server=utf8mb4_unicode_ci
      --init-connect='SET NAMES utf8mb4;'
      --innodb-flush-log-at-trx-commit=0
    environment:
      MYSQL_ROOT_PASSWORD: 1322655cf8ca7aaf2d57e428f4a07a366ce0384882d73f60d990832d05860967
      MYSQL_DATABASE: annyamodas_db
      MYSQL_USER: annyamodas_user
      MYSQL_PASSWORD: 5eba7d39cfb
      MYSQL_CHARSET: utf8mb4
      MYSQL_COLLATION: utf8mb4_unicode_ci
    ports:
      # Exponemos el puerto de MySQL solo a nuestra máquina local para seguridad
      - '127.0.0.1:3306:3306'
    volumes:
      # Persiste los datos de la base de datos
      - mysql-data:/var/lib/mysql
      # Script de inicialización
      - ./init.sql:/docker-entrypoint-initdb.d/init.sql
    networks:
      - annyamodas-network
    # Verificación de salud de la base de datos
    healthcheck:
      test: ["CMD", "mysqladmin", "ping", "-h", "localhost"]
      timeout: 20s
      retries: 10

  # Servicio de la Aplicación Next.js
  app:
    build: 
      context: .
      dockerfile: Dockerfile
    container_name: annyamodas-app
    restart: always
    ports:
      - '8080:3000'
    environment:
      NODE_ENV: production
      DATABASE_HOST: db
      DATABASE_USER: annyamodas_user
      DATABASE_PASSWORD: 5eba7d39cfb
      DATABASE_NAME: annyamodas_db
      JWT_SECRET: ea66f306553403a756d86c7b20f6712b4e4f05c8d4f4c801a0912f01ca5cf473
      # Variables para subida de imágenes (si usas un servicio local)
      UPLOAD_DIR: /app/public/uploads
    depends_on:
      db:
        condition: service_healthy # Espera a que MySQL esté ready
    networks:
      - annyamodas-network
    # Volumen para uploads (opcional, si quieres almacenar imágenes localmente)
    volumes:
      - ./public/uploads:/app/public/uploads

# Definición de volúmenes y redes
volumes:
  mysql-data:
    driver: local

networks:
  annyamodas-network:
    driver: bridge